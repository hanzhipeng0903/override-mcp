# API Override (MCP) 使用说明

一个浏览器扩展 + MCP server 组合，可以**拦截并改写任意网页的 HTTP 响应**——包括服务端返回 404、5xx，甚至完全连不上的请求。

可以由你手工操作，也可以用自然语言让 AI（Claude）通过 MCP 协议来驱动它。

---

## 目录

- [一、它能做什么](#一它能做什么)
- [二、架构](#二架构)
- [三、首次安装](#三首次安装)
- [四、让 Claude 能驱动它（推荐：HTTP 模式）](#四让-claude-能驱动它推荐http-模式)
- [五、典型工作流](#五典型工作流)
- [六、三种 override 类型](#六三种-override-类型)
- [七、规则字段速查](#七规则字段速查)
- [八、MCP 工具完整列表](#八mcp-工具完整列表)
- [九、HTTP 管理接口（不接 Claude 也能用）](#九http-管理接口不接-claude-也能用)
- [十、常见现象 / 已知限制](#十常见现象--已知限制)
- [十一、故障排查](#十一故障排查)
- [十二、配置项](#十二配置项)

---

## 一、它能做什么

| 场景 | 用法 |
|---|---|
| 后端接口还没写好，前端先开发 | `fulfill` 规则，整个接口直接 mock 成 200 |
| 接口偶尔 404/500，想稳定复现成功态 | `fulfill` 或 `passthrough_patch` |
| 真实接口可用，只想改返回里某几个字段 | `passthrough_patch` + `merge` 或 `jsonPatch` |
| 模拟网络异常（连接拒绝/超时/中断） | `block` 规则 |
| 让 AI "把刚才那个接口的 vip 字段改成 true" | 一句话搞定，AI 调 `tail_requests` → `mock_from_request` |

**关键能力**：404 / DNS 解析失败 / 服务器宕机的请求**也能 mock 成 200**——因为 `fulfill` 规则会在请求出门前就拦截，根本不让它发到网络。

---

## 二、架构

```
┌──────────────┐    stdio    ┌──────────────┐    WebSocket    ┌────────────────┐    CDP    ┌──────────┐
│ Claude Code  │ ──────────► │  MCP server  │ ◄────────────► │  Extension SW  │ ────────► │ Browser  │
│ (or any MCP  │             │  (Node.js)   │   ws://9876    │ chrome.debugger│           │   tab    │
│   client)    │             │              │                │                │           │          │
└──────────────┘             │  + HTTP 管理 │                └────────────────┘           └──────────┘
                             │  POST /call  │
                             └──────────────┘
                                    ▲
                                    │ (PowerShell / curl 也可直接调)
                                    │
                                  你 / 脚本
```

- **MCP server**：Node.js 进程，对 MCP 客户端通过 stdio 暴露 14 个工具，对内部启动一个 WebSocket 服务（端口 9876）等扩展连进来；同时在同一个端口上提供 HTTP 管理接口。
- **浏览器扩展**：Manifest V3 扩展，作为 WebSocket 客户端连到 MCP server。它通过 `chrome.debugger` 控制目标 tab，用 CDP 的 `Fetch` 域拦截/改写响应。
- **MCP 客户端**：Claude Code / Claude Desktop 等。它通过 MCP 协议调用工具，最终落到扩展执行。

---

## 三、首次安装

### 先选档位

| 档位 | 适用人群 | 装什么 | 能用什么 |
|---|---|---|---|
| **基础版** | 设计 / 运营 / 产品；不需要 AI 帮忙的同事 | 只装扩展 | popup UI 全部功能：抓包列表、一键 Mock、规则增删改、auto-attach。**够用 80%** |
| **完整版** | QA / 工程师 / 想用自然语言改接口的人 | 扩展 + MCP server + Claude Code 配置 | 基础版全部 + 让 Claude 一句话改接口 + 用 PowerShell/curl 脚本批量管理 |

> 同一台电脑可以**先装基础版**，未来想用 AI 再补完整版。完整版需要 Node.js（>=18），可以提前用 `winget install OpenJS.NodeJS.LTS` 装上。

---

### 档位 A：基础版（仅扩展，约 1 分钟）

1. 打开 Chrome（或基于 Chromium 的：Edge / Brave / 国内套壳），地址栏访问 `chrome://extensions`
2. 右上角打开 **开发者模式 / Developer mode**
3. 点 **加载已解压的扩展程序 / Load unpacked**
4. 选 `api-override-mcp\extension` 目录（**注意是 extension 子目录**，不是项目根目录）
5. 把 **API Override (MCP)** 扩展图标固定到工具栏

完成。点扩展图标打开 popup，或者点 popup 右上的 ↗ 在标签页全屏打开"控制台"视图。

> popup 顶部的 Bridge 状态会显示 disconnected（红色）——这是**预期的**，因为基础版没装 MCP server。**不影响任何 popup 功能**。这个红灯只是告诉你"AI 那条路现在不通"。

### 档位 B：完整版（扩展 + MCP + Claude Code）

先按档位 A 装好扩展，然后：

#### B1. 装 Node.js（已装可跳）

```powershell
winget install OpenJS.NodeJS.LTS
```

#### B2. 安装 MCP server 依赖

```powershell
cd C:\Users\hanzhipeng\Desktop\api-override-mcp\mcp-server
npm install
```

#### B3. 启动 MCP server（脚本已封装）

```powershell
.\ensure-running.ps1
```

幂等：已在跑就直接返回。看到 `[api-override] started on PID xxxx` 即 OK。
开机自启暂未做（阶段 2 计划），现阶段每次开机手动跑一次脚本即可。

#### B4. 接入 Claude Code

详见下面 **第四节：让 Claude 能驱动它**。

---

### 给同事打包

后续会出一键安装器（阶段 3 工作）。在那之前可以这样手工分发：

| 给谁 | 给什么 |
|---|---|
| 基础版用户 | 一个 zip：只含 `extension/` 目录 + 一份"如何加载"图文教程 |
| 完整版用户 | 全套项目目录（含 `mcp-server/`）+ Node.js 安装提示 + 一份配置 Claude Code 的说明 |

**零 Node 经验的同事直接走基础版**，避开任何命令行操作。

---

## 四、让 Claude 能驱动它（推荐：HTTP 模式）

> ⚠️ **不要把 api-override 写进 `~\.claude.json` 的 `mcpServers`**——
> 在 Claude Code (native, Bun 1.3.14 / Windows) 上会触发 Bun 内部段错误，
> 让 Claude Code 启动后立刻崩溃。原因不在我们的代码，但目前没法绕过。
> 因此**首选方案是让 Claude 通过本地 HTTP 接口调用我们的服务**，跳过 MCP stdio 通道。

### 4.1 启停 server（脚本已封装好）

```powershell
# 启动（幂等：已在跑就直接返回）
C:\Users\hanzhipeng\Desktop\api-override-mcp\mcp-server\ensure-running.ps1

# 停止
C:\Users\hanzhipeng\Desktop\api-override-mcp\mcp-server\stop.ps1
```

### 4.2 在 Claude 对话里使用

任意一段 Claude 对话里，让 Claude 跑下面这段（或直接告诉 Claude："**本机有一个 api-override 服务在 127.0.0.1:9876，启动脚本是 `Desktop\api-override-mcp\mcp-server\ensure-running.ps1`，POST /call {method, params} 调用工具，方法和参数见 `使用说明.md`**"）：

```powershell
# 1) 确保 server 在跑
C:\Users\hanzhipeng\Desktop\api-override-mcp\mcp-server\ensure-running.ps1

# 2) 定义 helper
function Call($method, $params=@{}) {
  $body = @{ method = $method; params = $params } | ConvertTo-Json -Depth 10 -Compress
  Invoke-RestMethod -Uri "http://127.0.0.1:9876/call" -Method Post -Body $body -ContentType "application/json"
}

# 3) 调任何工具
(Call 'status').result
(Call 'attach_active').result
(Call 'tail_requests' @{ n = 10 }).result
```

工具完整列表在第 8 节。

### 4.3 想让 Claude 在所有未来对话中都"知道"这个工具

把以下信息保存为一条 memory（让 Claude 帮你写）：

```
一个 reference 类型的 memory：
- 名称：api-override 工具位置
- 内容：本地有一个 API override 工具（项目位于
  C:\Users\hanzhipeng\Desktop\api-override-mcp）。启停脚本：
  mcp-server\ensure-running.ps1 / stop.ps1。HTTP 接口：
  POST http://127.0.0.1:9876/call {method, params}。
  方法清单见 使用说明.md 第 8 节。**不要**把它写进
  Claude Code 的 mcpServers 配置（会触发 Bun 段错误）。
```

这样在新会话里只要说「用 api-override 把 xxx 接口 mock 成 yyy」就能自动调用。

---

## 五、典型工作流

### 流程 A：让 Claude 一句话改接口

1. 你打开要改的网页
2. 点扩展 popup → **Attach to active tab**（黄条出现 = 正常）
3. 跟 Claude 说：「**把 `/api/me` 这个接口的返回里 vip 改成 true**」
4. Claude 自动：
   - 调 `tail_requests` 看到这个接口最近的真实响应
   - 调 `add_rule` 加一条 `passthrough_patch` 规则，merge `{vip: true}`
5. 你刷新页面，前端代码看到的就是 `vip: true`

### 流程 B：手动添加规则

跟 Claude 说：「**把 `*api/orders*` 这个接口 mock 成空数组**」

Claude 调用：

```js
add_rule({
  rule: {
    match: { url: '*api/orders*', method: 'GET' },
    action: {
      type: 'fulfill',
      status: 200,
      headers: { 'content-type': 'application/json' },
      body: '[]'
    }
  }
})
```

### 流程 C：基于真实响应建 mock

跟 Claude 说：「**把刚才那个用户接口的真实响应抓下来当 mock，但把 role 字段改成 admin**」

Claude 调用 `tail_requests` → `mock_from_request({reqId, patch:{role:'admin'}})` → 自动生成一条 `fulfill` 规则。

---

## 六、三种 override 类型

### `fulfill` — 完全 mock，不走网络

请求**根本不会发到服务器**，扩展直接返回伪造响应。
**最适合：后端没写、接口 404、模拟稳定的成功态。**

```js
{
  match: { url: '*api/v2/preferences*', method: 'GET' },
  action: {
    type: 'fulfill',
    status: 200,
    headers: { 'content-type': 'application/json' },
    body: '{"theme":"dark","lang":"zh"}'
  }
}
```

### `passthrough_patch` — 真实请求，改局部字段

请求正常打到服务器，扩展拿到真实 JSON 响应后，按你的 patch 改写再发给页面。
**最适合：接口可用，只想改某几个字段。**

```js
// 用 merge（深合并，最直观）
{
  match: { url: '*api/me' },
  action: {
    type: 'passthrough_patch',
    merge: { user: { vip: true, level: 99 } }
  }
}

// 或用 RFC 6902 JSON Patch（精准定位）
{
  match: { url: '*api/me' },
  action: {
    type: 'passthrough_patch',
    jsonPatch: [
      { op: 'replace', path: '/user/vip', value: true },
      { op: 'add',     path: '/user/badges/-', value: 'mvp' }
    ]
  }
}
```

`merge` 和 `jsonPatch` 可以同时用：先 merge，再 jsonPatch。

> ⚠️ 仅对 JSON 响应有效。非 JSON（HTML / 二进制）会被原样放行。

### `block` — 模拟网络失败

请求被阻断，前端会收到一个 NetworkError。
**最适合：测前端的错误处理、降级逻辑。**

```js
{
  match: { url: '*ads*' },
  action: { type: 'block', reason: 'ConnectionRefused' }
}
```

支持的 `reason` 值（CDP `Network.ErrorReason`）：

```
Failed | Aborted | TimedOut | AccessDenied
ConnectionClosed | ConnectionReset | ConnectionRefused
ConnectionAborted | ConnectionFailed
NameNotResolved | InternetDisconnected
AddressUnreachable | BlockedByClient | BlockedByResponse
```

---

## 七、规则字段速查

```js
{
  id:        'uuid',           // 自动生成，不用填
  enabled:   true,             // false = 暂停该规则但保留
  note:      '一句备注',       // 可选，给自己看的

  match: {
    url:    '*api/users*',     // glob: * 任意, ? 单字符
    // url: '/^https:\\/\\/x\\.com\\/api\\/.+/',  // 也支持 /regex/
    method: 'GET'              // 或 '*' 表示任何方法
  },

  action: { /* 三选一，见上节 */ },

  delay: 0,                    // 人为延时（毫秒），用来测 loading 态
  times: -1,                   // -1 = 不限次；N = 命中 N 次后自动 disable
  hits:  0                     // 自动累加，命中计数
}
```

URL 匹配示例：

| 模式 | 说明 |
|---|---|
| `*` | 匹配一切（慎用） |
| `*api/users*` | 包含 `api/users` 的任意 URL |
| `https://api.example.com/*` | 指定域名前缀 |
| `*?id=123*` | 带特定 query |
| `/^https:\/\/api\..+\/v2\//` | 正则（前后 `/` 包裹） |

---

## 八、MCP 工具完整列表

### 状态查询

| 工具 | 说明 |
|---|---|
| `status` | 桥接连接 / override 开关 / attached 数 / 规则数 |
| `list_tabs` | 列出所有 tab，标记哪些已 attach |

### Tab 管理

| 工具 | 参数 | 说明 |
|---|---|---|
| `attach` | `{tabId}` | 在指定 tab 上启动拦截（黄条出现） |
| `attach_active` | — | attach 当前活动 tab |
| `detach` | `{tabId}` | 停止拦截（黄条消失） |

### 规则管理

| 工具 | 参数 | 说明 |
|---|---|---|
| `list_rules` | — | 列出所有规则 |
| `add_rule` | `{rule}` | 添加规则 |
| `update_rule` | `{id, patch}` | 局部修改规则 |
| `remove_rule` | `{id}` | 删除规则 |
| `clear_rules` | — | 清空所有规则 |
| `enable` | — | 全局打开 override |
| `disable` | — | 全局关闭（规则保留） |

### 流量观察 & AI 自动建 mock

| 工具 | 参数 | 说明 |
|---|---|---|
| `tail_requests` | `{n, filter?:{url,method,status}}` | 最近 N 个请求（带 200 字符 body 预览）。`status` 支持前缀匹配，比如 `4` 命中所有 4xx |
| `get_request` | `{reqId}` | 获取某条请求的完整 req+res 详情 |
| `mock_from_request` | `{reqId, patch?}` | 用某条真实响应建一条 `fulfill` 规则；可选 `patch` 对原 JSON 做 deep-merge 后再存 |

`mock_from_request` 是**最实用的 AI 操作**：你说一句"把 /api/me 的 role 改成 admin"，AI 内部就是 `tail_requests` → 找到 reqId → `mock_from_request({reqId, patch:{role:'admin'}})`。

---

## 九、HTTP 管理接口（不接 Claude 也能用）

MCP server 在同一端口上还提供了一个简单的 HTTP 管理接口，方便用 PowerShell / curl 直接调用，不需要 MCP 客户端。

### 端点

| 路径 | 方法 | 说明 |
|---|---|---|
| `/status` | GET | bridge 健康检查 |
| `/call` | POST `{method, params}` | 调用任意 MCP 工具 |

### PowerShell 用法

```powershell
function Call($method, $params=@{}) {
  $body = @{ method = $method; params = $params } | ConvertTo-Json -Depth 10 -Compress
  Invoke-RestMethod -Uri "http://127.0.0.1:9876/call" -Method Post -Body $body -ContentType "application/json"
}

# 看状态
(Call 'status').result

# attach 当前活动 tab
(Call 'attach_active').result

# 列出 tab
(Call 'list_tabs').result | Where-Object { $_.attached }

# 看最近 10 条请求
(Call 'tail_requests' @{ n = 10 }).result

# 加规则：把所有 4xx 接口都 mock 成 200 空数组
$rule = @{
  match  = @{ url = '*api*' }
  action = @{ type = 'fulfill'; status = 200; body = '[]' }
}
(Call 'add_rule' @{ rule = $rule }).result

# 列规则、删规则
(Call 'list_rules').result
(Call 'remove_rule' @{ id = '<rule-id>' }).result
(Call 'clear_rules').result
```

### curl 用法

```bash
curl -X POST http://127.0.0.1:9876/call \
  -H "content-type: application/json" \
  -d '{"method":"status"}'
```

---

## 十、常见现象 / 已知限制

### ⚠️ 黄条："此扩展程序已开始调试此浏览器"

**这是必经之路。** 改 response body 必须用 `chrome.debugger`，Chrome 强制显示该提示，无法去掉。Detach 后立即消失。

### ⚠️ F12 / DevTools 不能和扩展共存

DevTools 和扩展用的是同一个 CDP 通道，会互相踢。Demo / 调试规则期间，**先关掉 F12**。要看页面控制台日志，可以临时 detach、开 F12、看完关掉、再 attach。

### ⚠️ chrome:// 等内部页面拦不到

包括 `chrome://extensions`、`chrome://settings`、新标签页等。Chrome 不允许扩展 attach 到这些页面。

### ⚠️ `tail_requests` 显示的 body 是服务器原始响应

这是个**双采集层**的副作用：

```
浏览器请求 → 服务器（真返回 unreadCount:1）
              ↓
       Network 域抓到 ←——— tail_requests 显示的就是这个
              ↓
       Fetch 域劫持 → 改成 unreadCount:999
              ↓
       页面实际收到 ←——— 这才是页面看到的
```

要确认 override 真的生效了：看 `list_rules` 里规则的 `hits` 是否 > 0；或者看页面 UI 是否变化。

### ⚠️ Service Worker / Web Worker 内部的 fetch

扩展默认只 attach 主页面 target。Worker 内的请求需要单独 attach 对应的 worker target，当前 MVP 暂未实现。

### ⚠️ 非 JSON 响应不能用 `passthrough_patch`

`passthrough_patch` 依赖 JSON.parse。HTML、二进制、流式响应等会被原样放行。这种情况用 `fulfill` 直接整体替换。

---

## 十一、故障排查

| 现象 | 原因 / 处理 |
|---|---|
| 加载扩展报"清单文件缺失" | 选错文件夹，应选 `extension` 子目录而不是项目根 |
| Popup 显示 Bridge: disconnected | MCP server 没启动；或端口 9876 被占。运行 `Get-NetTCPConnection -LocalPort 9876 -State Listen` 看看 |
| Attach 时报 "Cannot attach to chrome://" | 当前 tab 是 Chrome 内部页，换个普通网页 |
| Attach 时报 "Another debugger is already attached" | 该 tab 开了 F12，关掉 DevTools 再 attach |
| 加了规则但页面没变化 | 1. 检查 `list_rules` 里 `hits` 是否在涨；2. 规则的 url 模式是不是没匹配上（试着用 `*` 部分覆盖更广）；3. 该接口可能是 worker 发的，目前不支持 |
| `passthrough_patch` 不生效 | 该响应可能不是 JSON，看 `tail_requests` 里 `mimeType` 字段；非 JSON 请用 `fulfill` |
| Claude Code 看不到 `api-override` 工具 | `/mcp` 看下连接状态；配置文件路径是否正确；MCP server 启动报错可看 Claude 日志 |
| 改了 background.js 想热更新 | Chrome 扩展页面找到 API Override，点"刷新"图标；或卸载后重新加载 |

### 查后台日志

```powershell
# 实时看 MCP server 输出（如果是手动启动的）
# 直接看终端即可

# 看扩展 SW 的 console
# chrome://extensions → API Override (MCP) → "Service worker" 链接 → 打开 DevTools
```

---

## 十二、配置项

环境变量（启动 MCP server 时设置）：

| 变量 | 默认 | 说明 |
|---|---|---|
| `API_OVERRIDE_PORT` | `9876` | bridge 端口 |
| `API_OVERRIDE_HOST` | `127.0.0.1` | 绑定地址 |

> 改了端口的话，记得同时修改 `extension/background.js` 顶部的 `WS_URL` 常量，否则扩展连不上。

---

## 附：快速参考卡

```powershell
# 启动 server
cd C:\Users\hanzhipeng\Desktop\api-override-mcp\mcp-server; node index.js

# 检查端口
Get-NetTCPConnection -LocalPort 9876 -State Listen

# 一行加规则（PowerShell）
$body = '{"method":"add_rule","params":{"rule":{"match":{"url":"*api/me"},"action":{"type":"passthrough_patch","merge":{"vip":true}}}}}'
Invoke-RestMethod -Uri http://127.0.0.1:9876/call -Method Post -Body $body -ContentType "application/json"

# 清空规则
Invoke-RestMethod -Uri http://127.0.0.1:9876/call -Method Post -Body '{"method":"clear_rules"}' -ContentType "application/json"
```
