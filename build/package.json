{
  "name": "override-mcp-build",
  "version": "0.1.0",
  "description": "Build pipeline: bundle + obfuscate mcp-server and extension, output release zip.",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "node build.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "javascript-obfuscator": "^4.1.1",
    "archiver": "^7.0.1"
  }
}
